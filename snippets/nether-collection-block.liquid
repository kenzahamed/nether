{% comment %}
  Central dispatcher for Nether Premium Collection header blocks.

  Accepts:
  - block: {Object} Theme Editor block (required)
  - section: {Object} Collection section (required)
  - heading_tag: {String} Primary heading element (default: h2)

  Usage:
  {% render 'nether-collection-block', block: block, section: section, heading_tag: 'h2' %}
{% endcomment %}

{%- liquid
  assign heading_element = heading_tag | default: 'h2'
  assign subheading_element = 'h3'
  if heading_element == 'h3'
    assign subheading_element = 'h4'
  endif
-%}

{%- case block.type -%}
  {%- when '@app', 'eyebrow', 'heading', 'subheading', 'text', 'buttons' -%}
    {%- render 'nether-shared-header-block',
      block: block,
      class_prefix: 'nether-collection',
      heading_element: heading_element,
      subheading_element: subheading_element,
      motion_attr: 'data-nether-collection-animate',
      role_attr: 'data-nether-collection-role',
      text_role: 'intro'
    -%}

  {%- when 'category_highlight' -%}
    {%- render 'nether-collection-highlight', block: block, section: section -%}
{%- endcase -%}
