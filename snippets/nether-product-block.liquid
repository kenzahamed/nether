{% comment %}
  Central dispatcher for Nether Premium Product header blocks.

  Accepts:
  - block: {Object} Theme Editor block (required)
  - section: {Object} Product section (required)
  - heading_tag: {String} Primary heading element (default: h2)

  Usage:
  {% render 'nether-product-block', block: block, section: section, heading_tag: 'h2' %}
{% endcomment %}

{%- liquid
  assign heading_element = heading_tag | default: 'h2'
  assign subheading_element = 'h3'
  if heading_element == 'h3'
    assign subheading_element = 'h4'
  endif
-%}

{%- case block.type -%}
  {%- when '@app' -%}
    <div
      class="nether-product__app-block"
      data-nether-product-animate
      data-nether-product-role="app"
      {{ block.shopify_attributes }}
    >
      {% render block %}
    </div>

  {%- when 'eyebrow' -%}
    {%- if block.settings.text != blank -%}
      <p
        class="nether-product__eyebrow type-overline"
        data-nether-product-animate
        data-nether-product-role="eyebrow"
        {{ block.shopify_attributes }}
      >
        {{- block.settings.text | escape -}}
      </p>
    {%- endif -%}

  {%- when 'heading' -%}
    {%- if block.settings.heading != blank -%}
      <{{ heading_element }}
        class="nether-product__heading inline-richtext {{ block.settings.heading_size }}"
        data-nether-product-animate
        data-nether-product-role="heading"
        {{ block.shopify_attributes }}
      >
        {{- block.settings.heading -}}
      </{{ heading_element }}>
    {%- endif -%}

  {%- when 'subheading' -%}
    {%- if block.settings.subheading != blank -%}
      <{{ subheading_element }}
        class="nether-product__subheading inline-richtext {{ block.settings.subheading_size }}"
        data-nether-product-animate
        data-nether-product-role="subheading"
        {{ block.shopify_attributes }}
      >
        {{- block.settings.subheading -}}
      </{{ subheading_element }}>
    {%- endif -%}

  {%- when 'text' -%}
    {%- if block.settings.text != blank -%}
      <div
        class="nether-product__text rte {{ block.settings.text_style }}"
        data-nether-product-animate
        data-nether-product-role="intro"
        {{ block.shopify_attributes }}
      >
        {{ block.settings.text }}
      </div>
    {%- endif -%}

  {%- when 'buttons' -%}
    {%- if block.settings.button_label_1 != blank or block.settings.button_label_2 != blank -%}
      <div
        class="nether-product__buttons"
        data-nether-product-animate
        data-nether-product-role="buttons"
        {{ block.shopify_attributes }}
      >
        {%- if block.settings.button_label_1 != blank -%}
          {%- render 'button',
            label: block.settings.button_label_1,
            link: block.settings.button_link_1,
            style: block.settings.button_style_1,
            size: block.settings.button_size,
            disabled: block.settings.button_link_1 == blank
          -%}
        {%- endif -%}
        {%- if block.settings.button_label_2 != blank -%}
          {%- render 'button',
            label: block.settings.button_label_2,
            link: block.settings.button_link_2,
            style: block.settings.button_style_2,
            size: block.settings.button_size,
            disabled: block.settings.button_link_2 == blank
          -%}
        {%- endif -%}
      </div>
    {%- endif -%}

  {%- when 'product_highlight' -%}
    {%- render 'nether-product-highlight', block: block, section: section -%}
{%- endcase -%}
